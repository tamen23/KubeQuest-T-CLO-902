apiVersion: v1
kind: Service
metadata:
  name: {{ include "crementation.fullname" . }}
  labels:
    {{- include "crementation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crementation.selectorLabels" . | nindent 4 }}
