# TEMPLATE PLACEHOLDER — the encryptedData below is NOT real ciphertext and
# will fail to decrypt. Regenerate it with `kubeseal` against your running
# controller (see README.md in this directory). It's committed only to show
# the shape of a SealedSecret and that such a file is safe in git.
#
# NOTE: this file is intentionally NOT wired into infrastructure/kustomization.yaml
# because the placeholder can't decrypt; add it to the resources list once
# you've replaced it with real kubeseal output.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: demo-app-secret
  namespace: crementation
spec:
  encryptedData:
    API_TOKEN: AgBPLACEHOLDER0000ThisIsNotRealCiphertextRegenerateWithKubeseal==
  template:
    metadata:
      name: demo-app-secret
      namespace: crementation
    type: Opaque
